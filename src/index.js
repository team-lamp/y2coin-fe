import React from 'react';
import ReactDOM from 'react-dom';
import ProviderWrappers from './ProviderWrappers';
import './index.css';

ReactDOM.render(
	<React.StrictMode>
		<ProviderWrappers />
	</React.StrictMode>,
	document.getElementById('root')
);
